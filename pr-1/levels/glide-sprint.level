# Level — The Glide Sprint: The Glide under the hard one-minute clock
# (`time_limit = 60`, same rules as the other Sprints: the run ends the
# tick the clock hits zero, the score is the distance the minute allowed).
# No boulders and a fixed world — pure speed and throttle discipline, with
# the record ghost to pace against.
name = The Glide Sprint
description = Boulder-free speed. 60 seconds on the clock
scoring = distance
shafts = off
obstacles = off
pad_spacing = 130
seed = 0
time_limit = 60
